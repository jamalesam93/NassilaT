# Python dependencies for training/ scripts (validation, eval, smoke test).
# Full QLoRA training needs a separate GPU environment — see TRAINING_GUIDE.md.

requests>=2.31.0
huggingface_hub[cli]>=0.26.0

# Optional: only needed when running train_qlora_gemma4_e4b.py on a GPU machine
# torch>=2.1.0
# transformers>=4.45.0
# peft>=0.13.0
# bitsandbytes>=0.44.0
# accelerate>=0.34.0
# datasets>=3.0.0
# trl>=0.12.0
