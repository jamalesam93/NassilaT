# Python
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/

# Training artifacts (generated on Vast or locally)
training/outputs/
training/cache/

# Dataset secrecy — never upload training/eval data or field notes
training/data/
training/field-notes/

training/reports/**/seed_*_predictions.jsonl
training/reports/*_predictions.jsonl
training/reports/*_predictions*.jsonl
training/reports/tier3_*_predictions*.jsonl
training/reports/ab_*_v110/seed_*_combined_report.json
training/reports/**/*_predictions.jsonl
training/reports/**/*_predictions*.jsonl
*.log

# Generated codebase dump (repomix-style) — regenerate as needed, keep out of git
codebase.txt

# Secrets
.env
.env.*

# OS
.DS_Store
Thumbs.db

# Local Ollama Modelfiles (machine-specific GGUF paths — use HF README templates instead)
training/fixtures/Modelfile.*
